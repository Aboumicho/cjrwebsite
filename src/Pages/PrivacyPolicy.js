import React, { useEffect } from 'react';
export const PrivacyPolicy = () =>{
    return(
        <div>
            <p>Your data is stored in a secure way. We only store emails and let Facebook do the authentication</p>
        </div>
    )
}